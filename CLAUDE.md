# Penny — Developer Guide for Claude Sessions

This file is read at the start of every Claude Code session. It provides orientation. Deep reference lives in `docs/`. How-to patterns live in `.claude/commands/`.

---

## What this project is

**Penny** is an India-first personal wealth management PWA with an AI advisor called **Chip**. Privacy-first: local-first, AES-256 encrypted, zero trackers, zero backend in Phase 1.

- Working directory: `/Users/hemant.sharma/Projects/penny`
- Stack: React 19 + TypeScript (strict) + Vite + Tailwind v4 + Dexie.js + Web Crypto API
- Target: Mobile-first PWA, `max-w-[430px] mx-auto` desktop layout
- Currency/locale: `en-IN`, Indian Rupees (₹)
- **Monorepo since the mobile migration (July 2026):** pnpm workspace — `packages/core/` (platform-agnostic
  business logic, was `src/core/`+`src/lib/`) and `apps/web-legacy/` (this app's UI, was everything else in
  `src/`). See [`docs/plans/mobile-migration.md`](docs/plans/mobile-migration.md) and
  [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) before touching paths below — many still say `src/core/...`
  for brevity/history but now resolve to `packages/core/src/core/...`.

---

## Current milestone status

| Milestone                                                                              | Status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| -------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M0–M12: Foundation through portfolio enhancements                                      | ✅ Complete                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| M13: Financial calculators                                                             | ✅ Complete                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| M14: Finance news + Contact/Feedback                                                   | ✅ Complete                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| M15: UI polish + feature refinements                                                   | ✅ Complete                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Pre-Phase 1.5: Tracks 5, 1A–1E, 2, 3, 4, 6, 7 ✅ (Track 6 Step 3 skipped)**          | ✅ Complete                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Phase 1.5: Groups & Household OS — [plan](docs/plans/phase-1.5-groups-household-os.md) | 🚧 In progress. **Track 1 ✅** (pairwise IOU). **A ✅** API proxy (`penny-api-proxy`). **B ✅** client crypto (`device_keys`/`group_keys`/`sync_cursor`, `mergeBundle`). **C ✅** auth/identity (`workers/auth/`, signed-request auth, `signedFetch`/`claim`). **D ✅** backup+sync (`src/core/sync/`: Drive live, iCloud dormant, OPFS floor; Model B — user's own cloud). **E ✅ feature-complete + deployed, end-to-end verification pending** — E1–E5 + E5 tail (worker+crypto, lifecycle, split, sync, context switcher/dashboard/composer/settle/members, cash guard, share-with-group, vacation→group link, share-later, demo fixtures); `workers/groups/` deployed (reuses api-proxy KV; dedicated `penny_auth`/`penny_groups` D1s; **no R2** — event ciphertext inline in D1). `sync` is **env-gated** (`VITE_ENABLE_SYNC`); Groups require a **claimed username**. **Track F — Multi-Device, Sync & Recovery 🚧** ([plan](docs/plans/phase-1.5-track-F-multi-device-recovery.md)): **F1 ✅** phantom-claim fix (demo no longer fakes a claim; `claimed` honest); **F2 ✅** recovery hardening + restore-on-reinstall + account-start flow (Preview → Screen A cards → Screen B tabs → handle-recovery; mandatory username + claim at onboarding; deregister-failure surfacing); **F3 ✅** passphrase reclaim (Ed25519 challenge; auth worker `/recover/*` + migration `0003` — **worker needs redeploy+migrate before live verify**); **F4 device pairing/QR = next (discuss first)**; deferred: group-recovery-after-reclaim, groups-side account-delete cleanup. **Remaining before Phase 1.5 done: Track E live verification + F4 + Stage F closeout.** Resume/test: [track-E plan](docs/plans/phase-1.5-track-E-groups.md) "▶ Resume here"; recovery model: [track-F plan](docs/plans/phase-1.5-track-F-multi-device-recovery.md). Backend: [docs/BACKEND_STRATEGY.md](docs/BACKEND_STRATEGY.md). |
| **Mobile Migration — React Native (Expo)** — [plan](docs/plans/mobile-migration.md)    | 🚧 In progress, on branch `feat/rn-migration`. **Track 0 ✅ + Track 1 ✅ + Track 2 ✅** committed (repo restructuring, Expo app skeleton, storage+crypto adapters — on-device crypto/storage verification still owed for Track 1/2 specifically). **Track 3 ✅** core UI component library (all ~28 `components/ui/` ported to NativeWind, `Icon`/color/theme infra built, `ComponentGalleryScreen` verification tool). **Track 4 🚧 — Tier 1 shared infra ✅ + Subscriptions ✅ + Insurance ✅ + Loans ✅ + IOU ✅ + Goals ✅ + Accounts ✅ + Home ✅ + Portfolio ✅ + Expenses ✅**, all verified on a real Android emulator (`penny_pixel` AVD): `PrivacyContext`/`SettingsContext`/`ToastContext`/`useLoggedRepository` ported as reusable prerequisites (dependency survey split future modules into Tier 1/2/3 — see plan); `components/shared/` (`ListRow`/`DueDateBadge`/`FormModal`) ported alongside Insurance for Loans/IOU/Goals/Portfolio; several real bugs found+fixed on-device (missing safe-area top inset outside a `Stack.Navigator`; sibling `fullWidth` Buttons in a `flex-row` overflowing instead of splitting evenly, hit again in Portfolio's Equity modals and fixed the same way; a core `dueDateInfo()` CSS-var fallback with no RN equivalent; CSS Grid with no Yoga equivalent, including a 6-column amortization table and Portfolio's ~30+ sites, the most of any module; **seven bugs in shared `packages/core` itself**: `useTxnRefresh` and `useDataRefresh` both used browser-only `window` events — the first crashed and got a `.native.ts` fix, the second was pre-empted with the same fix before it crashed — `STATUS`'s colors are literal CSS var strings that silently failed across 3 already-shipped modules until IOU's on-device check caught the warning (fixed by swapping every mobile `STATUS.x` to `theme.x` in 7+ files), `marketDataClient.ts` called `localStorage` directly (reimplemented against mobile's `AsyncStorage` wrapper), `apiBase.ts` read Vite-only `import.meta.env` (fixed with a new `apiBase.native.ts`, same convention as the other `.native.ts` fixes), and `ipoClient.ts`/`npsClient.ts` both cached data via *synchronous* `localStorage` feeding async fetch functions — a harder variant needing `.native.ts` siblings with a session-only in-memory cache rather than a mechanical `AsyncStorage` swap). Loans drops its "Download XLSX" export (real capability gap, no native file-save/share flow built); IOU and Home are both scoped **personal-only** (their one `GroupContext`/Tier 2 dependency — Home's also drops `HomeGroupsCard` — is dropped rather than pulling in Groups' sync machinery early); **Portfolio, unlike IOU/Home, has no group dependency at all and was ported in full**. Home additionally required porting a prerequisite **Health** module (`FinancialHealthCard`'s CSS `conic-gradient` ring redesigned as a multi-arc `react-native-svg` ring, no RN equivalent existing) and pulled in three new native deps for the first time since Track 3 — `react-native-reanimated` (drives `MarketTicker`'s continuous-scroll marquee via a seamless `withRepeat`/`translateX` loop, replacing a CSS `@keyframes` marquee), `react-native-view-shot` + `expo-sharing` (Stories' share flow — snapshot a real off-screen RN `View` and open the native share sheet, replacing web's `<canvas>`+Web Share API; built now, not dropped, per explicit user decision), and `expo-linear-gradient` (Stories' gradient card backgrounds). Portfolio (the largest Track 4 module yet at the time, ~7,462 web lines) reused all of these — no new native deps needed — and added its own gotcha: several hand-rolled `fixed inset-0` modal overlays (Real Assets' `VehicleDetailModal`, Retirement's `NpsLifecycleDetail`/an `EpfAllTransactionsSheet`/an inline `RetirementSheets` popup, IPO's `IpoDetailModal`) rebuilt on the real ported `Modal` component instead of translated. **Expenses (~7,532 web lines) is CLAUDE.md's own flagged "hardest port"** (swipe gestures + an SVG chart) — both solved, not simplified: swipe-to-reveal row actions rebuilt on `react-native-gesture-handler`'s `ReanimatedSwipeable` (new native dep, needs `GestureHandlerRootView` at the app root); both SVG charts (annual bar+line, a donut reusing Health's exact ring technique) ported as plain `react-native-svg`, no charting library. Receipt photo capture (`expo-image-picker`+`expo-image-manipulator`, a new mobile-only `receiptImage.ts` helper since the input type differs fundamentally from web's `File`) and CSV/ZIP export (`core/export/exportCsv.native.ts` via `expo-file-system`+`expo-sharing`, reusing Home's Stories pattern) were both built now, not dropped. `EventModeContext` (vacation/trip mode) was ported as a real prerequisite rather than dropped — unlike every other module's droppable Groups dependency, event tagging is load-bearing across filtering/analytics/the header; `GroupContext` itself is still dropped everywhere (`ShareToGroupModal` skipped entirely). A real shared-component bug was found+fixed during on-device verification: `TabStrip`'s `scrollable` mode had an unconstrained horizontal `ScrollView` stretching to fill remaining vertical space, pushing tab labels down — fixed with `flexGrow: 0`, benefits every `TabStrip` consumer. Sub-page back buttons dropped for now (no real nav destination yet) — decided during Insurance, applies going forward. **Known limitation, applies to every module ported so far:** on-device saves throw "Session locked" — the temporary stand-in slot never runs onboarding/unlock, so the Data Master Key is never set; only render/layout verification is possible until real onboarding lands (fix exists via Demo Mode's `initialize()`, deliberately deferred per user decision — see plan). **Track C (identity/auth) prerequisite ✅** — ported/verified ahead of Groups, since (unlike every prior module) Groups' `claimed` gate needs a real server-verified identity with no local-only stand-in: `claimAccount`/`signedFetch`/device keypairs/`entitlement.native.ts`/real `AUTH_BASE`+`GROUPS_BASE` wiring (via a new `expo-constants` dependency) all confirmed working end-to-end on-device against the live `penny-auth` worker — including, as a side effect, the very first real on-device proof that DMK-based `EncryptedRepository` encrypt/decrypt actually works (every prior module hit "Session locked" before reaching it). See the plan's Track C progress-log entry for the crypto-primitive smoke test (P-256/Ed25519 via `react-native-quick-crypto`, all confirmed working) and a debugging false-alarm worth knowing about (a "Cipher.final failed" error that turned out to be a self-inflicted double-tap test artifact, not a real bug). **Groups ✅** — `GroupContext` + all 9 `features/groups/*` components ported, **plus** (user-requested) the three Groups integration points previously dropped as personal-only scoping in Home (`activeGroup → GroupDashboard`, `HomeGroupsCard`) and Expenses (`ShareToGroupModal`+swipe action, `familyGroupIds`, `EventsModal`'s `VacationGroupLink`) were restored — IOU stays personal-only, unchanged. Almost no new platform work needed (`packages/core/src/core/groups/*` was already platform-agnostic; `GroupContext`'s `localStorage`/`window`-event bits were already solved by Track C's `~/lib/storage`/`profileChangeBus.native.ts`); two new native-API swaps (`expo-clipboard` for invite-link copy, RN's built-in `Share` for the invite sheet) both confirmed working on-device, and `ContextSwitcher`'s hand-rolled dropdown was rebuilt on the real ported `Modal`. **Verified end-to-end on-device against the live `penny-auth`/`penny-groups` workers** (not just render-only) via a new `GroupsSmokeTestScreen` scratch tool: claim → create a group (real worker + key-wrap round-trip) → `GroupDashboard`/`GroupMembersModal` (invite link, `expo-clipboard`, real Android share sheet) → `SharedExpenseComposer` → Expenses' restored "Share with a group" toggle, all confirmed working, no crashes. One non-blocking bug found (`buildJoinLink`'s empty-origin fallback on RN, produces a hostless invite link) — flagged, not fixed, since it's shared `packages/core` code needing a real mobile deep-link scheme first. See the plan's Groups progress-log entry for full detail. **Onboarding + Settings/Security/Profile/Activity ✅ — Track 4 complete.** Real onboarding (13 screens, new `OnboardingNavigator`/`MainNavigator` `Stack.Navigator`s) finally sets a real Data Master Key on-device via a real UI, closing the standing "Session locked" limitation every prior module hit. Settings/Security/Profile/Activity ported alongside it. Two real bugs found + fixed during on-device Demo Mode verification, both severe enough to crash the emulator process itself, not just the app: (1) `schema.native.ts`'s shared `expo-sqlite` connection had no serialization, and `seedDemoData.ts`'s `Promise.all`-based seeding (~16 call sites) could silently drop writes and corrupt the native statement pool under concurrent load — fixed with a single FIFO queue over every DB operation, not a per-call-site patch; (2) `TransactionsTab.tsx` rendered its list unvirtualized (`View`+`.map()` in a `ScrollView`), mounting ~1,000 `SwipeableRow` gesture-handler instances at once with demo data — fixed by rebuilding it on a virtualized `SectionList`. See the plan's Onboarding progress-log entry for full detail. **Post-Track-4 restoration pass ✅/⚠️**: full app navigation wired (Insurance/Loans/IOU/Accounts/Subscriptions registered as real routes, Home's dead `onPress={() => {}}` entry points wired to them, a new shared `BackButton` restored via `PageHeader`'s `leading` slot on every pushed screen — confirmed on-device: Home → Loans renders the real page with a working back button) and IOU's Groups informational banner restored. **Loans' "Download XLSX" export is now fixed** — the original `xlsx` package never bundled under Metro (`require('fs')`/`require('stream')` in its CJS entry, unfixable via `resolver.extraNodeModules`); swapped to `write-excel-file`'s `/universal` entry point (depends only on `fflate`, a pure-JS zip lib, no Node builtins) — bundles clean, and its `Blob` output converts to `Uint8Array` via RN's `Response(blob).arrayBuffer()` (already polyfilled by `whatwg-fetch`) before the same `File.write()`+`expo-sharing` flow. Not yet on-device-verified (code-complete + typechecked/linted clean only). **ContextSwitcher wired ✅** (mounted in `MainTabs.tsx` above the tab navigator, gated by `hasEntitlement('sync')`; its claim/switch actions now navigate to real `Profile`/`Home` routes instead of no-ops) and a **top-of-screen double safe-area inset bug fixed** on Home/Portfolio/Expenses/Goals (those four tab-root screens were redundantly reserving the top inset `MainTabs`' own Stack header already consumes). **Feature-folder gap closed: 7 modules ported ✅** — a full audit of `apps/web-legacy/src/features/` (24 folders) vs `apps/mobile/src/features/` (16) found Feedback, Import, Backup & Restore, Cashflow, News, Calculators, and Tax Awareness had never been ported at all (Chip's rule-based insights dashboard was also missing at the time — ported in a later pass, see below; only real conversational Chip AI, Anthropic-backed, stays Phase 2). All 7 are now ported and wired, entry points restored across Home/Expenses/Settings that were previously flagged no-ops pending them. Notable new-dependency/platform work: `packages/core/src/core/sync/SyncProvider.native.tsx` (the automatic-backup engine now actually runs on mobile, via `AppState` instead of web's DOM `online`/`visibilitychange` events — needed adding `react-native` as a direct `packages/core` dependency) + `backupPrefs.native.ts` upgraded from in-memory-only to AsyncStorage-hydrated; `core/news/newsClient.native.ts` (RN has no `DOMParser` at all — a first for this migration — so RSS parsing is a small regex-based tag extractor instead); `@react-native-community/slider` (new native dep, Tax's Optimize "what-if" sliders); `TaxStoryModal`'s shareable image reuses Home Stories' `ShareCard`/`react-native-view-shot`/`expo-sharing` solution. See the plan's 2026-07-25 "ContextSwitcher wired... 7-module feature-folder gap closed" progress-log entry for full per-module detail. **On-device verification of the 7 new modules is now in progress**, testing across three surfaces at once (Android emulator, `apps/mobile` via `expo start --web`, and `apps/web-legacy` side by side). Two real bugs found + fixed: (1) RN-web crashed on `import.meta.env` — root cause was Expo's default Metro config excluding `'web'` from `resolver.platforms` entirely, so `.web.ts` platform-specific files (already added: `entitlement.web.ts`/`apiBase.web.ts`/`googleDriveProvider.web.ts`) were silently never picked up; fixed in `apps/mobile/metro.config.js` by explicitly appending `'web'` to that array (requires a full Metro restart, not hot-reloadable). (2) A systemic onboarding layout bug across **all 11** `OnboardingBack`-using screens — the back arrow (`position: absolute`) was rendered inside a padded `ScrollView`, and RN's `ScrollView` renders children in an *inner* content view distinct from the *outer* padded frame, so the button ended up offset ~40px from the true corner; fixed by moving `OnboardingBack` outside the `ScrollView` on all 11 screens (`SimulatedDashboardScreen` additionally got a one-off inline-with-heading redesign per user feedback, since it's the only one of the 11 with no hero icon to visually anchor the floating corner button). **One bug still open, blocking further on-device testing:** `TurboModuleRegistry.getEnforcing(...): 'QuickBase64' could not be found` — a native module nested under the crypto/Buffer polyfill stack isn't linked into the currently-installed APK, likely because this session's several `pnpm install`s (new slider dep, `react-native` added to `packages/core`) shifted native autolinking without a matching rebuild; a full `npx expo run:android` rebuild was recommended but not yet confirmed to fix it. **Next / resume here:** confirm the native rebuild clears the QuickBase64 error (if not, needs real autolinking investigation), then resume the on-device pass; the Loans XLSX bug also remains open (user wants to research alternative libraries later). See the plan's 2026-07-25 progress-log entries for full detail. **Post-Track-4 parity audit + restore (2026-07-25) ✅**: a direct web-legacy-vs-mobile comparison (prompted by user pushback that several silent scope cuts had shipped without clear callouts) found and fixed four real gaps: (1) **no persistent app-shell header at all** on mobile (`MainTabs` had `headerShown: false` on every tab, standing in for web's `AppShell` header) — fixed by giving `MainNavigator`'s `MainTabs` screen a real header (`PennyWordmark` logo + settings button on the left; new `PrivacyModeSwitcher`/`RemindersBell` ports, `components/privacy/`+`components/reminders/`, on the right), matching web's header exactly; (2) **theme picker missing** — mobile's `ThemeProvider` (Track 3) already had the palette infra but no UI ever called `setPreference`; `SettingsPage.tsx` now has the same 4-swatch grid (Light/Penny Blue/Dark/System) web has; (3) **font-scale setting missing entirely** — ported `FontScale`/`FONT_SCALE_MAP` into mobile's `SettingsContext.tsx` + a matching `Aa` picker grid in Settings; global application (unlike theme) is a real open gap flagged in `~/theme/fontScale.ts` — RN's `Text` (0.86) has no class prototype to patch like web's rem-based CSS var, so only an opt-in per-component `useFontScale()` hook exists so far, not automatic app-wide scaling; (4) **Chip's insights dashboard was never ported** — `apps/mobile/src/features/chip/ChipPage.tsx` now exists, wired into the Chip tab, same mock-insights scope as web today. Loans' XLSX export was also fixed in the same pass (see above). None of this has been on-device-verified yet (typecheck/lint clean only) — resume with on-device verification of all of the above, the QuickBase64 native-linking bug (still open, unrelated), and the 7-module on-device pass already in progress. **On-device verification (2026-07-25, later same day) found a critical bug beyond the QuickBase64 issue: `SessionGate` — the actual PIN-unlock screen — had never been ported at all**, so every cold launch skipped straight to the dashboard with no Data Master Key loaded, throwing "Session locked" everywhere. Fixed (full ~400-line port + `GroupProvider` relocated below the lock gate + a new `navigationRef.ts`), plus a cluster of smaller bugs found via direct use (Backup export crash, Exit Demo Mode wrong-screen + stuck-on-failure, `PrivacyModeSwitcher` non-functional dropdown, theme picker fully broken — root-caused to a `tailwind.config.js` color-key mismatch affecting ~33 screens, now fixed and on-device-verified). **A subsequent full parity sweep** (4 parallel agents, every `apps/web-legacy/src/features/` module vs mobile's) surfaced ~35 further findings, including one architectural one — privacy-mode background tinting doesn't cover full pages on mobile the way it does on web, since RN has no equivalent of the DOM's "stay transparent, let the parent show through." **A second, sharper re-sweep** (primed with that exact bug as a worked example) found 3 more real bugs beyond it: an exhaustive 23-file list for the background-tint fix, the Android hardware back-button bypassing the forced-PIN-reset lockout screen, and toasts rendering behind open modals. **All three were fixed and on-device-verified same day** (pixel-measured via `uiautomator dump`, not just eyeballed). **Font-scale's global-application gap was then investigated thoroughly and proven unfixable via NativeWind's `rem` observable** (concrete pixel measurement showed zero effect, both live and after a cold restart) — reverted to the honest opt-in-hook-only state; the real fix is a `<AppText>` wrapper migrated across the app, scoped as its own future task, not attempted. **Still open**: the original sweep's remaining ~32 items (6 more High-severity: Demo Mode banner missing, module-visibility-to-tab-bar not wired, `ExpenseForm`'s 3 dead-ends, Calculators' back-button-exits-feature bug, non-functional Backup destinations, stale Google-Drive onboarding handoff — plus the Medium/Low lists and `prefers-reduced-motion`). **See the plan doc's final "Where this session left off" section (bottom of the 2026-07-25 entries) for the single authoritative resume point** — several earlier progress-log entries from the same day have their own "resume here" phrasing that is now superseded by that final summary. That work was committed as `d8df285`. **All six High-severity parity-sweep items are now fixed** (Demo Mode banner, module-visibility-to-tab-bar wiring, `ExpenseForm`'s 2 dead-ends, Calculators' back-button bug, stale Google-Drive onboarding handoff, Backup destinations investigated/confirmed already-honest). **On-device verification of those six then found 3 more real bugs, all fixed same session**: `HomePage.tsx`'s non-group branch was the one tab-root screen still reserving the top safe-area inset a second time (`edges={['top']}` vs its siblings' `edges={[]}`), producing a wasted gap above the greeting (plus a stray unmatched `</>` fixed alongside it); `DemoModeBanner` was relocated per explicit user feedback from above `MainNavigator`'s header (web's `AppShell` ordering) to `MainTabs.tsx` just above `ContextSwitcher`, so the visible order is header → banner → "Personal" row → content; and a black gap above the header under the status-bar icons, caused by Expo's mandatory Android edge-to-edge rendering behind the system status bar while the custom `headerBackground` `View` was only sized to the header row — fixed by extending it upward via `useSafeAreaInsets().top`. Typecheck/lint/full test suite all clean throughout. **All Medium-severity punch-list items are now also fixed** (12 items: Accounts' Save button, shared `Button` ghost-variant text color, net-worth breakdown deep-links, Settings' Safe/Private colors, IOU's floating person-picker, 2 onboarding callout tints, Feedback's active-tab tint, News' theme-color hardcodes, Tax's bold lead-ins, Calculators' outcome-banner hierarchy, MoneyStory's gradient, PIN field styling) **plus the systemic no-date-picker gap** — a new shared `components/ui/DateInput.tsx` (`@react-native-community/datetimepicker`, a new dependency) wired into all 17 flagged call sites (DOB, policy renewal, IOU due dates, subscription "last charged", goal target dates, etc.). Typecheck/lint/prettier/full test suite all clean. On-device testing then hit the expected consequence of adding a new native dependency without a rebuild — `RNCDatePicker could not be found` — fixed via a full `npx expo run:android` native rebuild (same fix pattern as the earlier QuickBase64 incident), confirmed clean via logcat + a fresh screenshot. A user-flagged "wasted space" gap above the header was investigated via `adb shell dumpsys window displays` and confirmed **not a bug** — a genuine 136px/52dp OS status-bar reservation on this emulator (likely inflated by camera/mic privacy-indicator icons), not reclaimable by app content and not double-counted by the earlier header fix. **All 11 Low-severity parity-sweep items are now also fixed**: `ManageTagsPage` callout shade, a missing check-icon (username availability), missing icons (Claim & continue, shared-expense success line), a missing inset ring on a radio button (nested-circle technique, no RN `boxShadow: inset`), 3 onboarding screens' flattened spinners/gradient (`ActivityIndicator`, `expo-linear-gradient`), a real `privacy` theme token added to close the wrong-icon-color gap on activity privacy receipts (also fixed the same stand-in color in `PrivacyModeSwitcher`/`SettingsPage`), tap-to-view on heatmap cells (`Pressable` + toast), Goals badge capitalization + a new `Badge` icon slot (also fixes the missing sparkles icon), `ProgressBar` animation via Reanimated, and bullet markers on the tax disclaimer. Typecheck/lint/prettier/full test suite all clean throughout. This batch (High + Medium + Low + the two on-device bugs) was committed as `200eeba`. **`prefers-reduced-motion` is now fixed too** — a new `~/hooks/useReduceMotion.ts` (reads + subscribes to the OS accessibility setting) wired into Home's `MarketTicker`, showing one static copy with no animation when enabled. **Font-scale's global-application gap is also fixed for real, app-wide** — previously documented as needing a "100+ file manual migration," the user correctly pushed back on that framing and asked for real research into RN-native alternatives first. That research found RN 0.86's `Text` has no `defaultProps`/`render` seam to monkey-patch (confirmed by reading RN's own source, not assumed) — so a wrapper is unavoidable — but *applying* it didn't require touching call sites at all. A new `~/components/AppText.tsx` registers with NativeWind's own `cssInterop` (reads the already-`className`-resolved `fontSize`/`lineHeight` and multiplies both by the font-scale factor before handing off to real `Text`). **First attempt** used a one-time scripted codemod to physically rewrite `import { Text } from 'react-native'` to import `AppText` instead across all 189 files that had it — worked and verified on-device, but the user correctly flagged it as the wrong shape of fix (every file left with a slightly different import, and not self-enforcing — a new file written the normal way would silently opt out of scaling with no warning). **Reverted that and replaced it with a `metro.config.js` resolver alias** instead: a custom `resolveRequest` transparently redirects every `import { Text } from 'react-native'` written in app source (never `node_modules`, so libraries needing the real `Text` are untouched) to a new `~/lib/reactNativeShim.ts`, which re-exports everything from real `react-native` except `Text`. Zero per-file footprint, applies automatically to files that don't exist yet, can't be silently bypassed. Verified via `tsc -b`/`eslint`/full test suite (all clean) **and** on-device on the emulator: reloaded after a full Metro restart with cleared cache (required for `metro.config.js` changes), confirmed no regression at default scale, then set Settings' text-size picker to "A++" and confirmed real layout reflow, then reset to default and confirmed it shrank back — the same kind of live pixel evidence the earlier `rem.set()` attempt was disproven by, this time confirming success. Also built + installed clean on a real device (OnePlus 8T, USB via `expo run:android -d`) — build succeeded, no errors in `logcat` — user is doing the visual verification pass on that device separately. See the plan's 2026-07-26 progress-log entry for the full research writeup and the pivot from codemod to Metro alias. **Not yet committed.** **A third, much deeper parity sweep followed** (user-reported 7 concrete on-device bugs, root-caused, then 4 parallel agents covering every remaining `apps/web-legacy` module) and found real issues almost everywhere — 8 cross-cutting root causes (missing `open` color token affecting every destructive button app-wide; a dead `"surface"` CSS class silently no-op'ing 8+ card containers; unmemoized context values likely contributing to real Expenses lag; an unmemoized/uncached `Icon.tsx`; Groups hardcoding indigo instead of the live theme/privacy-mode accent color; scattered wrong-token color substitutions; unvirtualized large lists in Loans/News/Import/Cashflow/Groups; a silently-broken `font-mono`) plus standalone High items (Portfolio's Equity section lost its always-visible add-holding FAB; `ProfilePage`'s backup-nudge nests non-Text children inside a `Banner`'s `Text` wrapper, an invalid RN pattern) and a dozen-plus Medium/Low items — all documented in the plan's final entry, **nothing fixed yet**. User chose to stop the session here and resume fixing in a **new session** rather than pick a sequencing order now. **See the plan doc's "▶ Resume here (2026-07-26 session end)" section — the single authoritative pointer for what's uncommitted and what to ask the user first.** **Every finding from the third parity sweep is now fixed — the sweep is fully closed out.** Root causes A-F and H (new `open`/`tokens.ts` entry + `useModeAccentColor` hook; dead `.surface` class swapped to `bg-surface border border-theme`; `Privacy`/`Settings`/`EventMode` contexts memoized; `Icon.tsx` memoized per-instance via `useMemo`; scattered wrong-token color fixes; `font-mono` now resolves on Android) were fixed first, then **G (all 6 flagged lists — Loans/News/Import/Cashflow/Groups/Portfolio — converted to `FlatList`/`SectionList`, each screen restructured so the unbounded list owns its own scroll instead of nesting inside a `ScrollView`)**, then **both High-severity items (Portfolio's Equity FAB restored via a `forwardRef`+`useImperativeHandle` bridge; `ProfilePage`'s invalid `Banner`-wrapping-non-Text-children pattern fixed by manually replicating Banner's styling instead of nesting a `Button` inside its `Text`)**, then **all 12 Medium- and all 5 Low-severity items** (avatar gradients, account-type grid, SafeModeSettingsPage color, Cashflow bold emphasis + sparkline aspect ratio, FixedIncomeSection button placement, ghost-icon-button colors, curly quotes, translucent Edit-pill border, Portfolio's refresh-button shape, WrappedModal's footer anchor, stale doc comments). Typecheck/lint/prettier/full test suite all clean throughout; **not yet committed** — this session's change set spans the above plus the earlier font-scale/reduce-motion work, all still sitting in the working tree together. See the plan's 2026-07-26 "third parity sweep fully closed out" progress-log entry for full per-item detail. **Post-sweep, user-reported Expenses perf/UX bugs found + fixed**: Analytics' missing `ScrollView` (a real pre-existing gap, not from the sweep edits), tab-switch lag (`ExpensesPage.tsx` now lazy-mounts each tab once and keeps it alive via `display: 'none'` instead of unmounting on every switch, preserving `useExpenseAnalytics`'s memoized aggregates), a misleading "No transactions yet" flash during load (now a real skeleton, driven by a new `loading` flag from `useExpenses.ts`), and the Transactions date-header background (now matches the row cards' fixed surface color, confirmed intentional parity with web after asking the user, not tinted to the ambient page color). **The investigation into "why is it slow" then replaced the mobile storage engine entirely**: the user's Capacitor-vs-RN comparison (same demo dataset, same Dexie/IndexedDB code, buttery smooth in a WebView but not in real RN) isolated the bug to `schema.native.ts` specifically — `expo-sqlite`'s native binding required a single app-wide FIFO queue (added earlier to fix a real concurrent-access crash) that serialized every one of `useExpenses.ts`'s 8 independent table reads on mount, one at a time, on top of an async bridge round-trip per call. Dexie/IndexedDB itself was ruled out as a fix (IndexedDB is a browser-only Web API with no real implementation anywhere in RN — Dexie has nothing to wrap on-device). After a researched, sourced comparison of `expo-sqlite`+WAL, `@op-engineering/op-sqlite`, and `react-native-mmkv` (license/downloads/maintenance verified via live search, not assumed), **the storage engine is now `react-native-mmkv`** — chosen since its calls are fully synchronous JSI/Nitro with no shared connection object for concurrent access to corrupt, removing both the queue and the async-per-call overhead at the root, vs. patching expo-sqlite with WAL + a hand-built reader pool. Migration was fully contained behind the `RowStore<T>` storage seam designed for exactly this in Track 2 — zero changes to `EncryptedRepository`/`securityManager.ts`/any caller, and zero change to the per-record DMK envelope-encryption architecture (this adapter only ever sees opaque already-encrypted blobs). `expo-sqlite` removed outright from both `apps/mobile` and `packages/core` plus its `app.json` plugin entry; the old FIFO queue and SQLite migrations table are gone, not kept as a fallback. Typecheck/lint/prettier/full test suite clean; web build independently confirmed unaffected. **Not yet on-device-verified** — needs a native rebuild (new native dependency) and re-running the original bulk-seeding stress scenario to confirm the fix doesn't reintroduce the crash it's meant to avoid. See the plan's 2026-07-26 "user on-device report: Expenses perf/UX bugs found + fixed, then the storage engine itself replaced" progress-log entry for full detail. **Two more user-reported bugs then found + fixed**: the RN deprecation-warning banner wasn't actually gone (Metro's dev-mode Fast Refresh enumerates every module export regardless of what's imported — fixed with `ownKeys`/`getOwnPropertyDescriptor` traps hiding the 5 deprecated names from enumeration specifically); and the Transactions list went blank mid-scroll (a documented `VirtualizedList` failure mode — fast scroll outpacing the JS thread, likely dominated by each row's `ReanimatedSwipeable` mount cost — fixed with a memoized `TransactionRow`/`SwipeableRow`, `useCallback`'d list callbacks, and tuned `windowSize`/`maxToRenderPerBatch`/`updateCellsBatchingPeriod`). **The storage engine was then swapped a second time, MMKV → `@op-engineering/op-sqlite`**, after the user reported MMKV still didn't feel as snappy as web and pointed out (correctly) that a Capacitor build of the same Dexie-based web code ran smoothly on the same hardware. Corrected diagnosis: MMKV's synchronous calls block the JS thread for an entire bulk-read loop (1,000 rows = 1,000 inline calls), where SQLite's async calls dispatch to a native thread and only hand back the final result — the same off-thread shape Dexie/IndexedDB already has. Also fixed in the same pass: both prior RN adapters stored each encrypted row as a JSON-stringified `{id,iv,ciphertext}` wrapper (a layer Dexie never needed, since IndexedDB stores that object directly) — the ~27 always-`EncryptedRecord`-shaped tables now get real typed `id`/`iv`/`ciphertext` columns instead. WAL mode enabled; one connection only, per op-sqlite's own guidance. `react-native-mmkv`/`react-native-nitro-modules` removed outright. Typecheck/lint/prettier/full test suite clean; **not yet on-device-verified** (needs a native rebuild — new native dependency); on-device verification is the user's to do directly per their explicit instruction. Supersedes the "native apps" line item below — see the plan for locked decisions (single Expo codebase incl. web via `react-native-web`, NativeWind, `@op-engineering/op-sqlite`+`react-native-quick-crypto`). |
| Phase 2: Chip AI, cloud sync                                                           | ⏳ Future                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Phase 3: Regional languages, crypto, international equities                            | ⏳ Future                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

Full step-by-step milestone history → [`docs/MILESTONES.md`](docs/MILESTONES.md)
Phase 1.5/2/3 architecture decisions → [`docs/ROADMAP.md`](docs/ROADMAP.md)

---

## Architecture rules (enforced by ESLint)

Since the mobile migration's Track 0, these are enforced per-package (one shared `eslint.config.js` at the
repo root, with `files:`-scoped overrides — see that file for the exact paths):

1. **`@anthropic-ai/sdk`** may only be imported from `packages/core/src/core/ai-safety/anthropicClient.ts`
2. **`dexie`** may only be imported from `packages/core/src/core/db/`
3. **Feature modules** (`apps/web-legacy/src/features/`) must not cross-import — only from `core/`, `components/`, `context/`, `hooks/`, `lib/`
4. **`no-console`** is a warning — never log PII

Never disable these rules with `eslint-disable` comments.

---

## Encryption rules

- **Never access Dexie tables directly** from feature code
- Always use `EncryptedRepository<T>` from `packages/core/src/core/db/repository.ts`
- The Master Key (DMK) lives in memory only, non-extractable (`packages/core/src/core/crypto/keystore.ts`) — cleared on session expiry
- **Envelope encryption (Track 2):** a random Data Master Key (DMK) encrypts all data; it's wrapped independently by a passphrase-KEK (PBKDF2 600K) and a PIN-KEK (PBKDF2 200K). Changing passphrase/PIN re-wraps the DMK only — never re-encrypt data. Changing the passphrase requires the current passphrase. Never derive the data key directly from the passphrase. (See `docs/ROADMAP.md` → Track 2.)
- **DOB never leaves raw to AI** — use `deriveAgeBand()` (5-year band), never the exact date/age

---

## UI design

**All UI design lives in [`docs/DESIGN_GUIDELINES.md`](docs/DESIGN_GUIDELINES.md)** — the single source of truth: design ethos, navigation/layout & modal rules, reusable patterns (identity hero, in-field labels, icon-tile selector, grouped cards, danger zone), themes, design tokens, semantic theme/status colour utilities, and the mockup proposal workflow. **Read it before designing or adjusting any screen**, and add new patterns/rules there as they emerge — keep design guidance in that one doc, not scattered here.

Non-negotiables at a glance: centred modals (no bottom sheets); full-screen single-scroll over hidden tabs; a back button on every sub-page; **semantic tokens only — never hardcoded colours** (domain/brand accents excepted, as data in `core/*/meta.ts`).

## Shared utilities

- **All date logic lives in [`packages/core/src/lib/date.ts`](packages/core/src/lib/date.ts)** (keys, labels, `formatDate*`, `dueDateInfo`, `deriveAge`/`deriveAgeBand`, + `DAY_MS`/`startOfToday`/`daysUntil`/`daysBetween`). `lib/formatters.ts` is money/number only. Never re-implement day math or hardcode `86_400_000`. For DOB in AI context use `deriveAgeBand` (5-year band), never `deriveAge`/raw.

---

## Navigation structure

```
/onboarding/*       Pre-auth: splash, privacy promise, setup, demo, intro, simulated dashboard
/app/home           Home dashboard (default after onboarding)
/app/portfolio      Portfolio module
/app/expenses       Expenses module
/app/goals          Goals module
/app/insurance      Insurance (accessible from Home)
/app/chip           Chip AI chat
```

Bottom nav: Home · Portfolio · Chip (FAB, centred) · Expenses · Goals

---

## Key files

| File                                                          | Purpose                                                                   |
| -------------------------------------------------------------- | ------------------------------------------------------------------------- |
| `packages/core/src/core/db/schema.ts`                          | All Dexie stores — everything depends on this                             |
| `packages/core/src/core/db/types/index.ts`                     | All TypeScript types for DB records                                       |
| `packages/core/src/core/db/repositories.ts`                    | All repository instances                                                  |
| `packages/core/src/core/crypto/securityManager.ts`             | All reads/writes flow through this                                        |
| `packages/core/src/core/ai-safety/buildUserContext.ts`         | Only path to Anthropic                                                    |
| `packages/core/src/core/ai-safety/mockChip.ts`                 | All Phase 1 dev runs on this                                              |
| `packages/core/src/core/db/seedDemoData.ts`                    | Demo data seeding                                                         |
| `packages/core/src/core/db/activityLog.ts`                     | Activity log service — `logActivity`/`restoreActivity` (Track 4 Timeline) |
| `apps/web-legacy/src/hooks/useLoggedRepository.ts`             | Logging+Undo wrapper around `useRepository` for user mutations            |
| `packages/core/tests/pii-gate/piiGate.test.ts`                 | CI gate — never skip                                                      |
| `apps/web-legacy/src/context/PrivacyContext.tsx`               | Privacy mode — wraps entire app                                           |
| `apps/web-legacy/src/context/SettingsContext.tsx`              | Module visibility + font scale                                            |
| `apps/web-legacy/src/router/index.tsx`                         | All routes + AuthGuard                                                    |

---

## Documentation discipline (enforced in every session)

After completing any implementation step:

1. **Update `docs/features/<module>.md`** if the feature's capabilities, data model, or limitations changed
2. **Update `docs/SCHEMA.md`** if any Dexie store fields were added, changed, or removed
3. **Update `docs/ARCHITECTURE.md`** if new files, directories, hooks, or components were added
   - **Update `docs/DESIGN_GUIDELINES.md`** if a UI design pattern, rule, theme, or colour token was introduced or changed (it's the single source of truth for UI design — keep it there, not scattered)
4. **Sync status everywhere it's tracked** when a phase / track / step / module status changes — the `CLAUDE.md` milestone table, the matching row in `docs/MILESTONES.md` and `docs/ROADMAP.md`, and the **Status** line of the relevant plan in [`docs/plans/`](docs/plans/) (+ its index row). These must never disagree.
5. **Update `.claude/commands/penny-standards.md`** if new non-negotiable rules apply
6. **Update `docs/ROADMAP.md`** if any architectural decisions were made or changed
7. **Update the plan in `docs/plans/`** (and add a new one for any new phase / large multi-step track) if the approach or scope changed

Never mark a step as complete without checking this list.

---

## Where to find detailed information

| Topic                                                                   | Location                                                                               |
| ----------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| Product vision, users, competitive positioning                          | [`docs/BRD.md`](docs/BRD.md)                                                           |
| Full database schema with all fields                                    | [`docs/SCHEMA.md`](docs/SCHEMA.md)                                                     |
| Privacy rules, PII definitions                                          | [`docs/PRIVACY.md`](docs/PRIVACY.md)                                                   |
| **UI design** — ethos, patterns, themes, colours, mockup workflow       | [`docs/DESIGN_GUIDELINES.md`](docs/DESIGN_GUIDELINES.md)                               |
| Codebase map, component inventory, decision log                         | [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md)                                         |
| Phase 1.5/2/3 plans + backend decisions                                 | [`docs/ROADMAP.md`](docs/ROADMAP.md)                                                   |
| **Detailed approved phase/track plans** (why/what/how, step breakdowns) | [`docs/plans/`](docs/plans/)                                                           |
| Running `apps/mobile` (Expo Go / simulator / emulator / web)            | [`docs/RUNNING_MOBILE.md`](docs/RUNNING_MOBILE.md)                                     |
| Full milestone history with steps                                       | [`docs/MILESTONES.md`](docs/MILESTONES.md)                                             |
| Per-feature documentation                                               | [`docs/features/`](docs/features/)                                                     |
| Code standards + best practices                                         | [`.claude/commands/penny-standards.md`](.claude/commands/penny-standards.md)           |
| How to add a feature module                                             | [`.claude/commands/penny-feature-module.md`](.claude/commands/penny-feature-module.md) |
| Contributing guide                                                      | [`CONTRIBUTING.md`](CONTRIBUTING.md)                                                   |
