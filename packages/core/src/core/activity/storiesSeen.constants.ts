/** Storage key for the Home "stories" seen-set — shared between apps/web-react's and apps/mobile's
 *  independent StoriesRow.tsx implementations so the literal can't silently diverge between them. */
export const STORIES_SEEN_KEY = 'penny_stories_seen';
