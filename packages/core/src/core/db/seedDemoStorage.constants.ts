// Shared across seedDemoStorage.ts (web) and seedDemoStorage.native.ts — kept in exactly one place
// per the platform-variance-minimization principle (docs/ARCHITECTURE.md).
export const DEMO_SEED_KEY = 'penny_demo_seeded';
